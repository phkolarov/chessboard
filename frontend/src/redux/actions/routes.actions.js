
import {GetGameData} from "../../axios/game.requests";
import gameTypes from "../types/Game.types";

/**
 *
 * @returns {function(*): Promise<AxiosResponse<*> | void>}
 */
export const buildRoutes = () => async (dispatch, getState) => {

};
